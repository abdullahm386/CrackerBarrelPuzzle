Description: Cracker Barrel Puzzle game in prolog
NOTE: Must install the lambda package in library folder to use library(lambda) and run program. Can install with ?-pack_install(lambda) after starting SWILP

	